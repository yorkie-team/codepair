package config

import "testing"

func TestInitConfig(t *testing.T) {

	t.Run("Config with only environment variables test", func(t *testing.T) {
	})

	t.Run("Config with only yaml file test", func(t *testing.T) {
	})

	t.Run("Config with only Default values test", func(t *testing.T) {
	})

	t.Run("Environment override config file test", func(t *testing.T) {
	})

	t.Run("Config file override default values test", func(t *testing.T) {
	})
}
